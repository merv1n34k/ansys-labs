[project]
name = "geometry"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "cadquery>=2.7.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
